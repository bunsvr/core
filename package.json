{
  "name": "@bunsvr/core",
  "version": "0.0.10",
  "repository": {
    "url": "https://github.com/bunsvr/core"
  },
  "main": "index.js",
  "devDependencies": {
    "bun-types": "^0.4.0",
    "esbuild": "^0.16.10",
    "typescript": "^4.9.4"
  },
  "description": "Core server of BunSVR, which aims to be fast",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --minify --external:bun --outfile=index.js && tsc --declaration --outDir types --emitDeclarationOnly"
  },
  "type": "module",
  "types": "types/index.d.ts",
  "typedoc": {
    "entryPoint": "./src/index.ts",
    "displayName": "@bunsvr/core"
  }
}